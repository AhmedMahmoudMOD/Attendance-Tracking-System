
@{
    ViewData["Title"] = "StaffAttendance";
    Layout = "~/Views/Shared/_SLayout.cshtml";
}
<div id="container" class="container-fluid mt-3">
    <div class="row justify-content-center">
        <div class="col-6">
            <select id="stafftype" class="form-control" onchange="GetStaffAttendanceList(this.value)">
                <option selected value="1">Instructors</option>
                <option value="2">Employees</option>
            </select>
        </div>
        <div class="col-4">
            <input type="submit" value="Show" name="Submit" class="btn btn-success" onclick="GetStaffAttendanceList(stafftype.value)" />
            <button class="btn btn-dark MarkBtn" type="submit" onclick="MarkAbsence(stafftype.value)"><i class="fas fa-times fa-lg"></i></button>
        </div>
    </div>
</div>
<div id="AttList" class="mt-3 container row justify-content-around">
</div>

@section Scripts{
<script>
    function GetStaffAttendanceList(typeno){
        $.ajax({
            url: "/Security/GetStaffAttendanceList",
            data: { TypeNo: typeno},
            success: function (result) {
                $('#AttList').html('');
                $("#AttList").html(result)
            }
        });
    }
        function MarkAbsence(typeno) {
            $.ajax({
                url: "/Attendance/MarkStaffAbsence",
                data: { TypeNo:typeno },
                success: function (result) {
                    if (result) {
                        // Reload the page upon successful marking of absence
                        location.reload();
                    }
                }
            });
        }
    </script>

}

